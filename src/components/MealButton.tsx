import React from 'react';
import styled from '@emotion/styled';

interface MealButtonProp {
  image: string;
  label: string;
}
export default function MealCard(props: MealButtonProp) {
  const { image, label } = props;

  return <></>;
}
